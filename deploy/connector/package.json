{
  "name": "bizbox-connector",
  "private": true,
  "type": "module",
  "description": "BizBox connector: each member's AI agent works inside their own workspace folder (MCP over HTTP).",
  "scripts": { "start": "node server.mjs" },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.700.0",
    "@aws-sdk/s3-request-presigner": "^3.700.0",
    "@modelcontextprotocol/sdk": "^1.17.0",
    "zod": "^3.25.0"
  }
}
